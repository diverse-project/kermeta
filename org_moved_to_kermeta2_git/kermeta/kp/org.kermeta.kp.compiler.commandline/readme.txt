This project contains a Command Line kermeta 2 compiler designed to be run as standalone application
It uses a kp file as input (maybe later it will be able to use a merged km
Its core is also intended to be used as the base for the equivalent kermeta maven plugin