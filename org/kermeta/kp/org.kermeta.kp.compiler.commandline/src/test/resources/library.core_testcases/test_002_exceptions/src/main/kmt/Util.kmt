package rootPackage;

using kermeta_standard^kermeta::standard::*
using kermeta_standard^kermeta::io::StdIO => stdio

class MyException inherits kermeta::exceptions::Exception{

	operation create():MyException is do
		result := MyException.new
	end
}