package pkg{
	class Genericity{
		@main ""
		operation main() is do 
			var toto : pkg::Queue<kermeta::standard::String>.new
			toto.enqueue(kermeta::standard::String.new)
			kermeta::io::StdIO.writeln(toto.dequeue().toString)
		end
	}
	
	class Queue<G> {
		reference elements : kermeta::standard::oset G[*]
		operation enqueue(e : G) : kermeta::standard::Void is do
			elements.add(e)
		end
		operation dequeue() : G is do
			result := elements.first
			elements.removeAt(0)
		end
	}
}