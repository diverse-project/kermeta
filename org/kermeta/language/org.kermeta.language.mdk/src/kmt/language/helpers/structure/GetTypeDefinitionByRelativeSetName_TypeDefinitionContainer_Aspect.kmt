/* $Id:$ 
 * Creation : April 11, 2012
 * Licence  : EPL 
 * Copyright:
 * Authors  : 
 *            cguy
 */

package org::kermeta::language::structure;

require kermeta
require "platform:/lookup/org.kermeta.language.model/src/main/ecore/kermeta.ecore"

using kermeta::standard

aspect class ModelElementTypeDefinitionContainer
{
	operation getTypeDefinitionByRelativeSetName(set_name : Sequence<String>) : TypeDefinition is do
		var foundTypeDef : TypeDefinition
		var container : ModelElementTypeDefinitionContainer
		//If set_qname only contains one identifier, the TypeDefinition must be in this container
		if set_name.size()==1 then
			foundTypeDef := self.ownedTypeDefinition.detect{td|
				td.name == set_name.at(0)
			}
		//Else the TypeDefinition may be referenced by a ModelType contained by this package, or by a nested package
		else
			if (container.isVoid() and self.isInstanceOf(Package)) then
				container := self.asType(Package).nestedPackage.detect{p|p.name == set_name.at(0)}
			end
			if (not container.isVoid()) then
				var nextName : Sequence<String> init set_name
				nextName.removeAt(0)
				foundTypeDef := container.getTypeDefinitionByRelativeSetName(nextName)
			end
		end
		result := foundTypeDef
	end
}