package testConditional;

require kermeta

class String {}
class Integer {}
class Boolean {}


class Class1 {
	
	operation operation1() is do
		
		
		
		if a or b then v2 := "coucou" end
		
		if a or b and c or b then v2 := "coucou" end
		
		var b : Boolean init ! b().f
		
		if ! b then true
		end

		if ! a and b then true
		end
		if ! (a and b) then true
		end
		
		if a and b then true
		end
		
		if (a and b) and c then true
		end
		
		if (a and b) and  ! f().g then true
		end
		
		if (! a and b) and  ! f().g then true
		end
		
		if ! ( a and b or c ) and  ! f().g then true
		end
	end
	
}

