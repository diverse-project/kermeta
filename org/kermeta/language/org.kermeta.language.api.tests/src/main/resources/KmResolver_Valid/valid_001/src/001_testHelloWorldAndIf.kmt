/* $Id:$ 
 * Creation : September 14, 2010
 * Licence  : EPL 
 * Copyright:
 * Authors  : 
 *            cbouhour
 */
@mainClass "helloworld::HelloworldExample"
@mainOperation "sayHello"

package helloworldandif {
	class HelloworldExample
	{
		operation sayHello() : Void is do 
			stdio.writeln("Hello")
			var toto : Integer init 15
			toto := if (toto == 15) then
				16
			end
		end
	}
}

// for reflexivity
package org {
	package kermeta {
		package language {
			package structure {
				@CompilerIgnore "true"
				class Class {
				}
				
				@CompilerIgnore "true"
				class Object {
				}
			}
		}
	}
}

// for core framework
package kermeta{
	package io {
		@CompilerIgnore "true"
		singleton StdIO
		{			
			/**
			 * Writeln the object to standard output
			 */ 
			operation writeln(object : kermeta::standard::String) : kermeta::standard::Void is do
		    	
			end						
		}
	}
	package standard{

		/**
		 * The class Void 
		 * By convention it is a sub-type of everything
		 */
		@CompilerIgnore "true"
		class ~Void {
				
			/**
			 * Returns the String identifier '<void>'
			 */
			method toString() : kermeta::standard::String is do
			end			
		}


		@CompilerIgnore "true"
		class String {        
			
		}		
	}
}
