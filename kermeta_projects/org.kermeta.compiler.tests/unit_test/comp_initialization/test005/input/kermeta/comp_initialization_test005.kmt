@mainClass "comp_initialization_test005::Main"
@mainOperation "main"

@uri "http://comp_initialization_test005"
package comp_initialization_test005;

require kermeta

class Main
{
	// Initialization direct of a variable with type = A
    operation main() is do
    	var a : A init A.new
    	stdio.writeln("The variable a is correctly initialized: " + a.toString)
    end    
}

class A
{
}